{
  "ignoreCommand": "echo 'Deploys paused via vercel.json' && exit 0",
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "echo skip-root-install",
  "framework": "nextjs"
}
